//Vamos implementar uma lista simplesmente encadeada com informação somente do 1° elemento (nó).
//O nó:  informação / próximo
//vamos criar as nossas próprias bibliotecas com modularização.

#include <stdio.h>

int main () {
    int i, v[10];
    for (i=0; i<10; i++) {
        printf ("%d ", v[i]);
    }
    return 0;
}